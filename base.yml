version: '2'
services:
  ipfs:
    image: jlubbers08/ipfs
    networks:
     - ipfs
    dns: 
     - consul
    environment:
     - DC_NAME=ipfs
     - CONSUL_CLUSTER_IPS=consul
    volumes:
     - /data/
    ports:
     - 50001
     - 8080
     - 40001
    privileged: true
